/*The preprocessor read lines that start with # and execute the command*/
#include <stdio.h>  // in this case the preprocessor include the sdtio library.
                    //The standard angle brackets indicate that the file is to be found in the usual place.

int main(void) {
    printf("Start again from 0\n");
    return 0;       /* It causes the value zero to be returned to the operating system, which in turn may
                        use the value in some way, but is not required to do so.*/
}